from django.apps import AppConfig


class ImageIndexConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "image_index"
    verbose_name = "Time lapse cameras image index"
